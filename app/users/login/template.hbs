<h2>Login</h2>
<div class="login-form">
  {{#if errorMessage}}
  	<div class="alert alert-danger">
  	  <strong>Login failed:</strong> {{errorMessage}}
  	</div>
  {{/if}}
  <form {{action 'authenticate' on='submit'}}>
    <div class="form-group">
      <label for="identification" contenteditable="">Email</label>
      {{input  value=identification id="identification" type="text" class="form-control validate" }}
    </div>

    <div class="form-group">
      <label for="password">Password</label>
      {{input value=password id="password" type="password" class="form-control validate" }}
    </div>
    <button type="submit" class="btn btn-primary btn-block">Login</button>
  </form>
</div>
