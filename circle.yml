machine:
  node:
    version: v0.12.7

dependencies:
  override:
    - npm install
  post:
    - bower install

test:
  override:
    - testem ci

deployment:
  production:
    branch: master
    commands:
      - ember deploy --environment production
  staging:
    branch: staging
    commands:
      - ember deploy --environment staging