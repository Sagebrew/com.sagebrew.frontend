dependencies:
  override:
    - npm install
  post:
    - bower install
    - npm install testem -g

test:
  override:
    - echo 1

deployment:
  production:
    branch: master
    commands:
      - ember deploy --environment production
  staging:
    branch: staging
    commands:
      - ember deploy --environment staging